# shared-gsquiz-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-gsquiz-types` to build the library.

## Running unit tests

Run `nx test shared-gsquiz-types` to execute the unit tests via [Jest](https://jestjs.io).
